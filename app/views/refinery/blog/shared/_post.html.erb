<!-- This is what is iterated around for each post in the blog.. -->
<% if post.live? %>
  <figure>
    <% if !post.item.nil? %>
      <%= image_tag post.item.url %>
    <% end %>
    <figcaption>
      <%= link_to post.title, refinery.blog_post_path(post) %>
    </figcaption>
  </figure>
<% end %>